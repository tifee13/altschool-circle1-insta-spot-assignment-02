import React from 'react';

const Gallery = () => (
  <main className="main" role="main">
    <section className="container" aria-label="Photo gallery"></section>
    <dialog className="previewable-modal modal" aria-label="previewable-modal"></dialog>
  </main>
);

export default Gallery;